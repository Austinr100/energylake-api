# Test-only dependencies (not installed on Railway). Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
# Run tests with: pytest
-r requirements.txt
pytest>=8.0
httpx>=0.27          # required by fastapi.testclient.TestClient
